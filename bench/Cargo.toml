[package]
edition = "2024"
name = "bench"
publish = false
version = "0.0.0"

[dependencies]
serde_json = "1"
serde = { version = "1.0.210", features = ["derive"] }
jtoo = { path = "../jtoo", features = ["time"] }
time = { version = "0.3", default-features = false, features = ["serde-human-readable"] }

[dev-dependencies]
